Parfait 👍
Voici le programme COMPLET en un seul bloc, prêt à copier-coller, + les commandes exactes à exécuter.

Je pars sur le cas le plus robuste :
➡️ lecture du fichier .ods,
➡️ prise de la première colonne (peu importe son nom),
➡️ nettoyage (espaces, lignes vides),
➡️ dédoublonnage,
➡️ affichage + sauvegarde.


---

1️⃣ Commandes à exécuter

Installation des dépendances (une seule fois)

pip install pandas odfpy

Lancer le script

python dedoublonner_hls.py


---

2️⃣ Programme Python COMPLET

👉 fichier : dedoublonner_hls.py

import pandas as pd

# ==============================
# CONFIGURATION
# ==============================
FILE_PATH = "CQs HLS avec doublon_20260115160236.ods"
OUTPUT_TXT = "hls_dedoublonnees.txt"
OUTPUT_CSV = "hls_dedoublonnees.csv"

def main():
    # Lecture du fichier ODS
    df = pd.read_excel(FILE_PATH, engine="odf")

    if df.empty:
        raise ValueError("Le fichier est vide.")

    # Lecture de la première colonne (HLS)
    hls_column = df.iloc[:, 0]

    # Nettoyage + suppression des doublons
    hls_unique = (
        hls_column
        .dropna()                 # supprime les lignes vides
        .astype(str)              # force en string
        .str.strip()              # enlève les espaces
        .unique()                 # dédoublonnage
    )

    hls_list = list(hls_unique)

    # Affichage
    print("\nHLS dédoublonnées :")
    print("-" * 30)
    for hls in hls_list:
        print(hls)

    # Sauvegarde TXT
    with open(OUTPUT_TXT, "w", encoding="utf-8") as f:
        for hls in hls_list:
            f.write(hls + "\n")

    # Sauvegarde CSV
    pd.DataFrame({"HLS": hls_list}).to_csv(
        OUTPUT_CSV,
        index=False,
        encoding="utf-8"
    )

    print("\nFichiers générés :")
    print(f"- {OUTPUT_TXT}")
    print(f"- {OUTPUT_CSV}")

if __name__ == "__main__":
    main()


---

3️⃣ Résultat attendu

✅ Liste des HLS sans doublons affichée dans le terminal

✅ Fichier hls_dedoublonnees.txt

✅ Fichier hls_dedoublonnees.csv



---

Variantes possibles (si tu veux aller plus loin)

dédoublonnage insensible à la casse

tri alphabétique

comptage des occurrences

sélection par nom de colonne

export Excel .xlsx


Dis-moi ce que tu veux, je t’adapte le script directement 🔧