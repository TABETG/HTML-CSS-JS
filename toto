
### Ce que j’ai ajouté (et seulement ajouté)

* **Deux blocs de flags AER_SOC-1529** (en haut de `searchIncident(...)` et `searchIncidentMoral(...)`) :

  * `allowFCC_BDF` = `true` si `RequestTypeEnum` ∈ {`FCC_BDF`, `BOTH_BNP_BDF`, `BDF_ONLY`}.
* **Encadrement des appels existants** :

  * Dans `searchIncident(...)` (PP) : le bloc

    ```java
    final FCCResponse gppi = fccClient.searchGlobalPP(...);
    callStaticsService(...);
    result.addAll(mapFCCResponsePP(...));
    ```

    est désormais exécuté **uniquement** si `allowFCC_BDF` est vrai. Sinon on log un *skip*.
  * Dans `searchIncidentMoral(...)` (PM) : même principe autour de

    ```java
    fccClient.searchGlobalPM(...);
    callStaticsService(...);
    result.addAll(mapFCCResponsePM(...));
    ```
* **Aucune ligne supprimée** : le code original reste intact, simplement placé dans des `if` conditionnels.
* **Indentation** homogène pour éviter tout warning/erreur de formatage.

si tu veux, je fais la même chose sur `FICPService` (toujours en « ajouts uniquement ») 👌
